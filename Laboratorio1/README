# Simulador de dados usando PIC12F683 y LEDs

La carpeta informe contiene el reporte del laboratorio, la carpeta ./src contiene los códigos fuentes y archivos de simulación que llevan a la simulación.

## Uso

Ubicándose en la carpeta ./src se ejecuta el comando make:

```bash
make
```
Esto copila el programa. Una vez copilado se abre el archivo circuito.simu en el programa SimulIDE.
Se carga el firmware main.hex en el MCU y se simula.